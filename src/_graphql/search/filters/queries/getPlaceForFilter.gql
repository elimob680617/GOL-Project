query getPlaceForFilter($filter: GenericFilterRequestFilterReqInputType!) {
  placeSearchQueryHandler(filter: $filter) {
    listDto {
      items {
        id
        title
      }
      count
    }
  }
}
