query gertIndustryForFilter($filter: GenericFilterRequestFilterReqInputType!) {
  industrySearchQueryHandler(filter: $filter) {
    listDto {
      items {
        id
        title
      }
      count
    }
  }
}
