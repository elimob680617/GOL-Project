import { ReactNode } from 'react';

import WizardList from 'src/sections/profile/user/wizard/WizardList';

const wizardRoutes: Record<string, ReactNode> = {
  'wizard-list': <WizardList />,
};

export default wizardRoutes;
